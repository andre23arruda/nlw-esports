export type Ad = {
	id: number
    week_days_array: string[]
    created_at: string
    name: string,
    years_playing: number,
    hour_start: number,
    hour_end: number,
    use_voice_channel: boolean,
}
